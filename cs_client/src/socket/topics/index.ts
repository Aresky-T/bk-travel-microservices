export const WS_CONVERSATION_TOPIC = (conversationId: number) => `/topic/conversation/${conversationId}`
export const WS_STAFF_TOPIC = (staffId: number) => `/topic/staff/${staffId}`
export const WS_STAFF_STATUS_TOPIC = (staffId: number) => `/topic/staff/status/${staffId}`