export const WS_CONVERSATION_TOPIC = (conversationId) => `/topic/conversation/${conversationId}`
export const WS_STAFF_TOPIC = (staffId) => `/topic/staff/${staffId}`
export const WS_STAFF_STATUS_TOPIC = (staffId) => `/topic/staff/status/${staffId}`